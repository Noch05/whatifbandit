% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{imputation_preparation}
\alias{imputation_preparation}
\title{Outcome Imputation Preparation}
\usage{
imputation_preparation(
  current_data,
  data_cols,
  imputation_information,
  whole_experiment,
  blocking,
  delayed_feedback,
  current_period
)
}
\arguments{
\item{current_data}{A \code{data.frame} or \code{data.table} with only observations from the current sampling period.}

\item{whole_experiment}{Logical; if \code{TRUE}, uses all past experimental data for imputing outcomes.
If \code{FALSE}, uses only data available up to the current period. In large datasets or with a high number
of periods, setting this to \code{FALSE} can be more computationally intensive, though not a significant
contributor to total run time. Default is \code{FALSE}.}

\item{delayed_feedback}{Logical; if \code{FALSE}, assumes instanteneous feedback for outcomes, as soon as a treatment is assigned, the outcome is realized and known.
If \code{TRUE}, delayed feedback is assumed, so as soon as treatment is assigned, a potential outcome is realized, but it is not known to the simulation, until a certain date.
When re-computing the adaptive assignment probabilities via Thompsom Samplings or UCB1 outcomes that have not been observed on the date the assignment are treated as failures. a}
}
\value{
A named list containing:
\itemize{
\item \code{current_data}: A \code{tibble} or \code{data.table} containing \code{impute_block} column to guide the outcome imputations
\item \code{impute_success}: A \code{tibble} or \code{data.table} object containing probabilities of success by \code{treatment_block} used to impute
outcomes. Subsetted from the \code{\link[=imputation_precompute]{imputation_precompute()}} output.
\item \code{impute_dates}: Named date vector by treatment condition, containing the dates of success
to impute if delayed_feedback is FALSE. Subsetted from the \code{\link[=imputation_precompute]{imputation_precompute()}} output.}
}
\description{
Executes all preparations necessary to impute outcomes for
each iteration of the simulation loop. Adds an additional column to the current data,
subsets necessary information from the \code{\link[=imputation_precompute]{imputation_precompute()}} output, and checks to ensure
compatibility with \code{\link[randomizr:block_ra]{randomizr::block_ra()}}.
}
\details{
The goal of this function is to set up the imputation procedure and prevent
errors from occurring. \code{\link[randomizr:block_ra]{randomizr::block_ra()}} does not see the names
of the probabilities passed per block, so the imputation information must be subsetted
to contain only the treatment blocks which exist in a given period.

\code{impute_block} is the observation's new treatment block, combining any
blocking variables with their new treatment assigned via the Multi-Arm-Bandit
procedure.
}
\keyword{internal}
