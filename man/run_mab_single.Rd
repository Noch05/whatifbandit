% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_api.R
\name{run_mab_single}
\alias{run_mab_single}
\title{Run a single MAB simulation iteration}
\usage{
run_mab_single(
  sim_type,
  algorithm,
  estimators = c("aipw", "ipw", "ols"),
  control_augment = 0,
  random_assign_prop = 0,
  prior_periods = NULL,
  delayed_feedback = FALSE,
  discount_rate = 1,
  conditions,
  blocking,
  clustering,
  col_names,
  ndraws = 5000,
  keep_data = FALSE,
  keep_models = FALSE,
  contrasts = NULL,
  verbose = FALSE,
  r = 1,
  imputation_information = NULL,
  whole_experiment = NULL,
  time_model = NULL,
  time_model_args = NULL,
  p = NULL,
  n = NULL,
  dt = NULL,
  blocks = NULL,
  clusters = NULL,
  equal_probs = NULL,
  assignment_dates = NULL,
  simulate_dates = NULL,
  period_idxs,
  data = NULL
)
}
\arguments{
\item{sim_type}{String; Type of simulation to conduct, either \code{"resim"}, \code{"param"}, or "\verb{test"},
for a resimulated rct, simulation from population parameters, or simulation for the randomization joint test.}

\item{algorithm}{A character string specifying the MAB algorithm to use. Options are
\code{"Thompson"}, \code{"UCB1"}, or "static", ignoring case. Algorithm
defines the adaptive assignment process. For more details on these specific algorithms see
\href{https://www.jstor.org/stable/2332286}{Thompson 1933};
\href{https://doi.org/10.1023/A:1013689704352}{Auer et al. 2002};
\href{https://proceedings.mlr.press/v23/agrawal12.html}{Agrawal and Goyal 2012};
\href{https://arxiv.org/abs/1402.6028}{Kuleshov and Precup 2014} and
\href{https://arxiv.org/abs/1904.07272}{Slivkins 2024}.}

\item{estimators}{Character vector; Which estimators to compute, can include \code{"aipw"}, \code{"ipw"},
"ols", and any combination
of them in a vector.}

\item{control_augment}{Minimum proportion of each treatment assignment wave guarenteed to recieve the treatment labelled as \code{"Control"}. Ranges from 0 to 1,
and the default is 0. It is not recommended to use this in conjunction with \code{random_assign_prop}.}

\item{random_assign_prop}{Proportion of each treatment assignment wave assigned via static, equal
probabilities of assignment instead of the adaptively updated probabilities generated by the MAB
algorithm. Ranges from 0 to 1, default is 0. It is not recommended to use this in conjunction
with \code{control_augment}. When batch sizes are small and \code{random_assign_prop * batch_size < 1},
this value instead represents a probability instead of a raw proportion. Under clustering, its
possible that \code{random_assign_prop} may not reflect the true proportion of units selected due to
the constraint that all units within a cluster have to be assigned the same treatment.}

\item{prior_periods}{A positive integer; number of previous periods to use in the treatment
assignment model. Default is \code{NULL}, where all prior periods are considered. See below for
details.}

\item{delayed_feedback}{Logical; if \code{FALSE}, assumes instanteneous feedback for outcomes, as soon
as a treatment is assigned, the outcome is realized and known. If \code{TRUE}, delayed feedback is
assumed, so as soon as treatment is assigned, a potential outcome is realized, but it is not
known to the simulation, until a certain date. When re-computing the adaptive assignment
probabilities via Thompsom Samplings or UCB1 outcomes that have not been observed on the date the
assignment are treated as failures. a}

\item{discount_rate}{Rate for discounting observations from earlier periods when updating
assignment probabilities. A value between 0 and 1, where outcomes from \code{k} periods ago are
weighted by \code{discount_rate^k}. Default is 1 for no discounting.}

\item{conditions}{Character vector of treatment condition labels. If a control group is specified
the \code{names} attribute should be present with the control group labelled \code{"control"}.}

\item{blocking}{Logical; Whether or not treatment blocking is occuring}

\item{clustering}{Logical; Whether or not treatment clustering is occuring}

\item{col_names}{List holding the columns required from the provided data as strings and symbols.}

\item{ndraws}{Number of draws used to approximate Thompson sampling probabilities. Used only when
direct calculation fails or overflows. Default is 5000 but can be raised or lowered depending on
performance and accuracy concerns.}

\item{keep_data}{Logical; Whether or not to keep the final data from each trial. Recommended
\code{FALSE}. When\code{ r = 1} the final data is always kept and reported.}

\item{keep_models}{Logical; Whether or not to keep the final IPW and OLS vcov/models from each trial. Recommended
\code{FALSE}. When\code{ r = 1} models are always kept and reported. Required to be \code{TRUE} to compute
arbitrary pairwise contrasts.}

\item{contrasts}{Character string specifying which pairwise contrasts to
precompute after each replication. One of \code{"control"} (each arm vs. control arm),
\code{"best"} (each arm vs. the MAB-selected best arm),
\code{"both"}, or \code{"all"} (all \code{choose(k, 2)} pairwise comparisons, expensive
for large \code{k}). All contrasts are tested under the two-sided null of no difference.
Defaults to \code{NULL}, arbitrary contrasts can be computed if \code{keep_models == TRUE}}

\item{verbose}{Logical; whether or not to print intermediate messages. Default is \code{FALSE}.}

\item{r}{Positive integer; number of replications (under different random seed). Replications of
the MAB procedure on a fixed dataset provides important diagnostic information on the
stochasticity/variance of the re-simulation method. Replications can be conducted in parallel, by
setting an appropriate \code{\link[future:plan]{future::plan()}}. See details below.}

\item{imputation_information}{Object created by \code{\link[=precompute_imputation]{precompute_imputation()}} containing the conditional
means and success dates
for each treatment block to impute from.}

\item{whole_experiment}{Logical; if \code{TRUE}, uses all past experimental data for imputing
outcomes. If \code{FALSE}, uses only data available up to the current period. In large datasets or
with a high number of periods, setting this to \code{FALSE} can be more computationally intensive,
though not a significant contributor to total run time. Default is \code{FALSE}.}

\item{time_model}{An optional function with signature \verb{function(n, conditions, successes, current_period, blocks = NULL, clusters = NULL, ...)}
that returns a vector of \link[lubridate:period]{lubridate::period} objects which will then be added to \code{dates_of_assignment} to produce \code{success_date}. Used to simulate delayed feedback mechanism
during the trial, so outcomes are imperfectly observed. Only used when\code{dates_of_assignment} is also supplied. Dates can be generated even when \code{delayed_feedback == FALSE},
but they will not be used. Default \code{NULL}. Other optional arguments Cannot share names with arguments in \code{\link[furrr:furrr_options]{furrr::furrr_options()}}.}

\item{time_model_args}{Arguments passed to \code{time_model} function.}

\item{p}{The true probabilities of success for each treatment arm. Specified as an matrix,
where \code{rownames(p)} are the treatment. If there is a control condition, specify its rowname as \code{"Control"}
labels, and \code{colnames(p)} are the cluster or block labels, e.g.
\code{matrix(c(0.5, 0.3, 0.5, 0.6), nrow = 2, ncol = 2, dimnames(list(c("Control", "T1"), c("B1", "B2"))))}.
Probabilities are accessed as \code{p[treatment, block]}.
With blocks and clusters utilize the clusters for the columns because clusters are fully nested in blocks.
For no clusters or blocks simply use a matrix with 1 column.}

\item{n}{A positive integer. Total number of units to simulate.}

\item{dt}{Logical. If \code{TRUE} returns a \code{\link[data.table:data.table]{data.table::data.table()}}; otherwise returns a \code{\link[tibble:tibble]{tibble::tibble()}}. Default \code{FALSE}.}

\item{blocks}{A named numeric vector of block membership probabilities (must sum to 1), where \code{names(blocks)}
are the block labels. Units are assigned to blocks via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no blocking.}

\item{clusters}{Cluster membership probabilities. Can be:
\describe{
\item{Numeric vector}{A named vector where \code{names(clusters)} are the cluster labels e. g. \code{C(C1 = 0.4, C2 = 0.6)}.
Used when there is not blocking.}
\item{Named list of vectors}{A named list where \code{names(clusters)} are block labels, and each element is a named vector
of per-block cluster proportions, e.g.
\code{list(B1 = c(C1 = 0.4, C2=0.6), B2 = c(C3 = 0.2, C4 = 0.8))}
Clusters are accessed as \code{clusters[[block]][cluster]}. Insided each block, cluster proportions must sum to 1, and the same cluster cannot appear in multiple blocks.}
}
Units are assigned to clusters via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no clustering.}

\item{equal_probs}{Vector of equal assignment probabilities.}

\item{assignment_dates}{An optional \code{Date} vector of dates representing when units are assigned.
If shorter than \code{n} it is recycled and sorted. If NULL` (default) no assignment dates are recorded.}

\item{simulate_dates}{Logical; whether or not new success dates should be generated using
\code{time_model}. Does not guarantee these new dates are used for assignment, \code{delayed_feedback} controls
that behavior.}

\item{period_idxs}{List containing vectors which map their entries to the starting row and ending
row of each period.}

\item{data}{\code{tibble} or \code{data.table} holding necessary information to complete the simulation.
Also used to store simulation outputs.}
}
\value{
A named list containing:
\itemize{
\item \code{final_data}: The processed \code{tibble} or \code{data.table}, with the trial's results.
\item \code{bandits}: A \code{tibble} or \code{data.table} containing the UCB1 or Thompson Sampling values for each period.
\item \code{assignment_probs}: A \code{tibble} or \code{data.table} containing the probability of being assigned each
treatment arm at a given period.
\item \code{assignment_quantities}: A numeric vector of the total number of observations assigned to each
treatment arm.
\item \code{estimates}: A \code{tibble} or \code{data.table} containing the estimates of the specified estimators for
each treatment arm.
\item \code{call}: \code{NULL}; initialized for later assignment.
\item \code{args}: \code{NULL}; initialized for later assignment.
\item \code{furrr}: \code{NULL}; initialized for later assignment.
}
}
\description{
Runs a single iteration of a MAB simulation. Intended to be
called either directly for \code{ r = 1} or as the mapped function inside
\code{\link[furrr:future_map]{furrr::future_map()}}\code{for} r > 1\verb{. Handles all }sim_type`.
}
\keyword{internal}
