% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mab_trial_sim.R
\name{extract_success_prob}
\alias{extract_success_prob}
\title{Extract Success Probabilities Per-Unit}
\usage{
extract_success_prob(p, treatments, other_idx, blocking, clustering)
}
\arguments{
\item{p}{The true probabilities of success for each treatment arm. Specified as an matrix,
where \code{rownames(p)} are the treatment
labels, and \code{colnames(p)} are the cluster or block labels, e.g.
\code{matrix(c(0.5, 0.3, 0.5, 0.6), nrow = 2, ncol = 2, dimnames(list(c("T1", "T2"), c("B1", "B2"))))}.
Probabilities are accessed as \code{p[treatment, block]}.
With blocks and clusters utilize the clusters for the columns because clusters are fully nested in blocks.
For no clusters or blocks simply use a matrix with 1 column.}

\item{treatments}{A character or factor vector of treatment assignments of
length \code{n}.}

\item{other_idx}{Character vector of block or cluster assigents to be used as the
additionnal index for extracting from \code{p}.}

\item{blocking}{Logical; Whether or not treatment blocking is occuring}

\item{clustering}{Logical; Whether or not treatment clustering is occuring}
}
\value{
A numeric vector of length \code{n} containing the per-unit success
probability.
}
\description{
Looks up the success probability for each unit given their treatment
assignment and, optionally, their block and/or cluster membership. Handles
all supported \code{p} structures.
}
\keyword{internal}
