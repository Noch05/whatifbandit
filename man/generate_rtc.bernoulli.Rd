% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic.R
\name{generate_rtc.bernoulli}
\alias{generate_rtc.bernoulli}
\alias{generate_rct.bernoulli}
\title{Generate Synthetic RCT Data for MAB Re-Simulation}
\usage{
generate_rct.bernoulli(
  n,
  t,
  p,
  simple = TRUE,
  dt = FALSE,
  dates_of_assignment = NULL,
  time_model = NULL,
  ...
)
}
\arguments{
\item{n}{Number of observations to generate.}

\item{t}{Number of treatments.}

\item{p}{Numeric vector of length \code{t} containing true probabilities of success for each treatment. \code{names(p)} provide the names of the treatment groups.}

\item{dt}{Logical; whether to return a \code{data.table} or a \code{tibble}. Default is \code{FALSE}}

\item{dates_of_assignment}{Optional \code{Date} vector containing dates of assignment for each observation.
If its length is less than \code{n} provided dates will be recylced.}

\item{time_model}{Optional user-specified function that models the time from treatment until success for successful observations.
The function must return a \code{period} object. It should accept \code{n}, treatment assignments and outcomes as its first 3 arguments,
respectively. Additional arguments may be supplied via \code{...}.}

\item{...}{Additional arguments to \code{time_model}.}

\item{simple;}{Logical; Whether to use simple random assignment or complete random assignment. Default is \code{TRUE}}
}
\value{
\code{tibble} or \code{data.table} with \code{n} rows, containing the following columns:
\itemize{
\item \code{assignment_dates}: Dates of assignment for each observation.
\item \code{treatments}: Assigned treatment for each observation.
\item \code{success}: Whether or not treatment was successful for each observation.
\item \code{success_dates}: Dates of success for each observation calculated as \code{assignment_dates + time_model(treatments, success, ...)}.
\item \code{id}: Row number.
}
}
\description{
Generates compatible \code{tibble} or \code{data.table} for use in \code{single_mab_simulation()}
based on specified input parameters.
}
\examples{
# 2-Arm Design; Complete Randomization

generate_rct.bernoulli(n = 1000, t = 2, p = c(0.3, 0.7), simple = FALSE)

# Linear relationships in treatment

generate_rct.bernoulli(n = 1000, t = 10, p = (1:10) * 0.1 - 0.05)

# Modelling Success Dates

time_model <- function(n, t, s) {
  # Specific times for each treatment
  time <- data.table::fcase(
    t == "T1" , rexp(n, rate = 5)                 ,
    t == "T2" , rgamma(n, shape = 2, rate = 2)    ,
    t == "T3" , rweibull(n, shape = 3, scale = 4)
  )

  time <- ifelse(s == 1, round(time) * lubridate::days(3), NA)
  return(time)
}
# Using with `single_mab_simulation()`

set.seed(100)
result <- generate_rct.bernoulli(
  n = 10000,
  t = 10,
  p = c(0.3, 0.6, 0.45),
  dates_of_assignment = lubridate::ymd("2023-04-15") +
    0:24 * months(1),
  time_model = time_model
) |>
  single_mab_simulation(
    assignment_method = "date",
    time_unit = "month",
    period_length = 1,
    algorithm = "Thompson",
    prior_periods = "All",
    perfect_assignment = FALSE,
    whole_experiment = FALSE,
    blocking = FALSE,
    data_cols = c(
      id_col = "id",
      success_col = "success",
      condition_col = "treatment",
      date_col = "assignment_date",
      assignment_date_col = "assignment_date",
      success_date_col = "success_date"
    )
  )
print(result)
result$estimates[1:3, ]
}
