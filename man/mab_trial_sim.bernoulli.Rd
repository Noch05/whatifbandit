% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{mab_trial_sim.bernoulli}
\alias{mab_trial_sim.bernoulli}
\title{Simulate an Adaptive Trial With Bernoulli Distributed Outcomes}
\usage{
mab_trial_sim.bernoulli(
  n,
  t = n,
  p,
  algorithm,
  blocks = NULL,
  clusters = NULL,
  control_augment = 0,
  random_assign_prop = 0,
  period_sizes = NULL,
  prior_periods = "all",
  dt,
  ndraws = 5000
)
}
\arguments{
\item{n}{A positive integer. Total number of units to simulate.}

\item{t}{Total number of assignment periods. Positive integer. Default is \code{t = n} for pure sequential (one unit per period) assignment.
The sizes of each period will be equal as \code{n \%/\% t}, except for the last
period which will be \code{n \%\% t} in the case \code{n \%\% t != 0}, when \code{period_sizes = NULL}.}

\item{p}{The true probabilities of success for each treatment arm, where \code{length(p)} is the number of
treatment arms. Can be:
\describe{
\item{Numeric vector}{A named vector where \code{names(p)} are the treatment
labels, e.g. \code{c(T1 = 0.2, T2 = 0.4)}. Used when there are no blocks
or clusters.}
\item{Named list of vectors}{A named list where \code{names(p)} are the treatment
labels and each element is a named vector of per-block or per-cluster
success probabilities, e.g.
\code{list(T1 = c(B1 = 0.2, B2 = 0.4), T2 = c(B1 = 0.3, B2 = 0.5))}.
Probabilities are accessed as \code{p[[treatment]][block]}.}
\item{Nested named list}{A doubly-nested named list used when both blocks
and clusters are present. The outer names are treatment labels, the inner
names are block labels, and each inner most vector is a named vector of
per-cluster success probabilities. e.g.
\verb{list(T1 = list(B1 = c(C1 = 0.2, C2 = 0.4), B2 = c(C1 = 0.7, C2 = 0.4)) T2 = list(B1 = c(C1 = 0.5, C2 = 0.5), B2 = c(C1 = 0.3, C2 = 0.6)))}
Accessed as \code{p[[treatment]][[block]][cluster]}.}
}
All probability values must be between 0 and 1.}

\item{algorithm}{Assignment algorithm, determines how probabilities of assignment
are updated each period. Either \code{"thompson"} for Thompson Sampling, \code{"ucb1"} for
the UCB1 algorithm, or \code{"static"} for uniform, non-adaptive assignment. Not case sensitive.}

\item{blocks}{A named numeric vector of block membership probabilities (must sum to 1), where \code{names(blocks)}
are the block labels. Units are assigned to blocks via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no blocking.}

\item{clusters}{Cluster membership probabilities. Can be:
\describe{
\item{Numeric vector}{A named vector where \code{names(clusters)} are the cluster labels e. g. \code{C(C1 = 0.4, C2 = 0.6)}.
Used when there is not blocking.}
\item{Named list of vectors}{A named list where \code{names(clusters)} are block labels, and each element is a named vector
of per-block cluster proportions, e.g.
\verb{list(B1 = c(C1 = 0.4, C=0.6)), B2 = c(C3 = 0.2, C4 = 0.8))}
Clusters are accessed as \code{clusters[[block]][cluster]}. Insided each block, cluster proportions must sum to 1, and the same cluster cannot appear in multiple blocks.}
}
Units are assigned to clusters via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no clustering.}

\item{period_sizes}{Numeric vector of \code{length(t)}, with the specific number of units to be assigned in each period. Used when it is required to assign different numbers of units
to treatment across the periods of the trial.}

\item{dt}{Logical. If \code{TRUE} returns a \code{\link[data.table:data.table]{data.table::data.table()}}; otherwise returns a \code{\link[tibble:tibble]{tibble::tibble()}}. Default \code{FALSE}.}
}
\description{
Simulates a response-adaptive randomized experiment with Bernoulli
distributed outcomes. At each period, observed outcomes are used to update assignment
probabilities according to the specified \code{algorithm}. \code{algorithm = "static"} is the non-adaptive
uniform baseline, where probabilities of being assigned to one treatment is the same as any other.
}
\details{
When blocking and/or clustering are specified, these assignments will be randomly pregenerated before the start of the adaptive sequential assignment. These arguments allow simulating a trial
when there may be hetergenous outcomes across a treatment block or treatment cluster, so different assignment probabilities can be provided for the same treatment, depending on the block and/or cluster
of a unit.

Clustering is challenging under an adaptive trial, because then the probabilities of assignment being adaptive can have little impact on the new assignments, given that an early treatment assignment to a cluster
must remain the same across the whole trial. As such this function assumes clusters do not persist across periods, so are all respecitvely assigned at the same time. If a design is provided, as such periods are
too small for the clusters to fit in a period, its possible for assignment to vary within the same cluster in the experiment.
}
