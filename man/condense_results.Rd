% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{condense_results}
\alias{condense_results}
\title{Condenses results of repeated simulations.}
\usage{
condense_results(dt, keep_data, mabs)
}
\arguments{
\item{dt}{Logical; Whether to output \code{data.table}s or \code{tibble}s. When\code{ r * number_of_periods > 100000}, \code{dt = TRUE}, even if the user passed data is not a
\code{data.table}.}

\item{keep_data}{Logical; Whether or not to keep the final data from each trial. Recommended
\code{FALSE}. When\code{ r = 1} the final data is always kept and reported. @param check_args Logical;
Whether or not to robustly check whether arguments are valid. Default is TRUE, and recommended
not to be changed. @param ... Additional named arguments passed to \code{\link[furrr:furrr_options]{furrr::furrr_options()}}}

\item{mabs}{List of outputs from repeated \code{\link[=run_mab]{run_mab()}} calls.}
}
\value{
A named list containing
\itemize{
\item \verb{final_data:} \code{tibble} or \code{data.table} containing the nested \code{tibble}s/\code{data.table}s from each trial. Only provided when \code{keep_data = TRUE}.
\item \code{bandits}: A \code{tibble} or \code{data.table} containing the UCB1 values or Thompson Sampling posterior distributions for each period and trial. Wide format,
each row is a period, and each columns is a treatment.
\item \code{assignment_probs}: A \code{tibble} or \code{data.table} containing the probability of being assigned each treatment arm at a given period and trial. Wide format,
each row is a period, and each columns is a treatment.
\item \code{estimates}: A \code{tibble} or \code{data.table} containing the all estimates and variances for each arm.
Long format, treatment arm, and estimate type are columns along with the mean estimates
and variance estimates.
\item \code{ipw_vcov}: A 3d arrary containing the covariance matrix of coefficients of IPW estimates of each trial.
}
}
\description{
Condenses results of repeated simulations.
}
\details{
This function iterates over every element in \code{mabs} and extracts the required element to place in a condensed list
for the final output.
}
\keyword{internal}
