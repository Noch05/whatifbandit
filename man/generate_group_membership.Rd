% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_mab.R
\name{generate_group_membership}
\alias{generate_group_membership}
\title{Generate Block or Cluster Memberships}
\usage{
generate_group_membership(n, group, blocks = NULL)
}
\arguments{
\item{n}{A positive integer. Number of units to assign.}

\item{group}{A named numeric vector or named list (see \code{\link[=simulate_mab]{simulate_mab()}}) of assignment probabilities.
When blocks and cluster are together, clusters must be fully nested in blocks.
\code{names(group)} are used as the condition labels (block or cluster names).}

\item{blocks}{A named numeric vector of block membership probabilities (must sum to 1), where \code{names(blocks)}
are the block labels. Units are assigned to blocks via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no blocking.}
}
\value{
A factor of length \code{n} with levels corresponding to \code{names(group)} or \code{NULL} if \code{group = NULL}.
}
\description{
Takes a named probability vector for blocks or clusters and uses
\code{\link[randomizr:complete_ra]{randomizr::complete_ra()}} to randomly assign each of \code{n} units to a
block or cluster according to those probabilities.
}
\keyword{internal}
