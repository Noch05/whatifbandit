% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{generate_rct.bernoulli}
\alias{generate_rct.bernoulli}
\title{Generate a Bernoulli RCT dataset}
\usage{
generate_rct.bernoulli(
  n,
  p,
  dt = FALSE,
  blocks = NULL,
  clusters = NULL,
  dates_of_assignment = NULL,
  time_model = NULL,
  ...
)
}
\arguments{
\item{n}{A positive integer. Total number of units to simulate.}

\item{p}{The true probabilities of success for each treatment arm. Can be:
\describe{
\item{Numeric vector}{A named vector where \code{names(p)} are the treatment
labels, e.g. \code{c(T1 = 0.2, T2 = 0.4)}. Used when there are no blocks
or clusters.}
\item{Named list of vectors}{A named list where \code{names(p)} are the treatment
labels and each element is a named vector of per-block or per-cluster
success probabilities, e.g.
\code{list(T1 = c(B1 = 0.2, B2 = 0.4), T2 = c(B1 = 0.3, B2 = 0.5))}.
Probabilities are accessed as \code{p[[treatment]][block]}.}
\item{Nested named list}{A doubly-nested named list used when both blocks
and clusters are present. The outer names are treatment labels, the inner
names are block labels, and each inner most vector is a named vector of
per-cluster success probabilities. e.g.
\verb{list(T1 = list(B1 = c(C1 = 0.2, C2 = 0.4), B2 = c(C1 = 0.7, C2 = 0.4)) T2 = list(B1 = c(C1 = 0.5, C2 = 0.5), B2 = c(C1 = 0.3, C2 = 0.6)))}
Accessed as \code{p[[treatment]][[block]][cluster]}.}
}
All probability values must be between 0 and 1.}

\item{dt}{Logical. If \code{TRUE} returns a \code{\link[data.table:data.table]{data.table::data.table()}}; otherwise returns a \code{\link[tibble:tibble]{tibble::tibble()}}. Default \code{FALSE}.}

\item{blocks}{A named numeric vector of block membership probabilities (must sum to 1), where \code{names(blocks)}
are the block labels. Units are assigned to blocks via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no blocking.}

\item{clusters}{Cluster membership probabilities. Can be:
\describe{
\item{Numeric vector}{A named vector where \code{names(clusters)} are the cluster labels e. g. \code{C(C1 = 0.4, C2 = 0.6)}.
Used when there is not blocking.}
\item{Named list of vectors}{A named list where \code{names(clusters)} are block labels, and each element is a named vector
of per-block cluster proportions, e.g.
\verb{list(B1 = c(C1 = 0.4, C=0.6)), B2 = c(C3 = 0.2, C4 = 0.8)}
Clusters are accessed as \code{clusters[[block]][cluster]}. Insided each block, cluster proportions must sum to 1, and clusters across blocks
should be different.}
}
where \code{names(clusters)} are the cluster labels.
Units are assigned to clusters via \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}. Pass \code{NULL} (default) for no clustering.}

\item{dates_of_assignment}{An optional vector of dates representing when units are assigned.
If shorter than \code{n} it is recycled and sorted. If NULL` (default) no assignment dates are recorded.}

\item{time_model}{An optional function with signature \verb{function(n, treatments, success, blocks = NULL, clusters = NULL, ...)}
that returns a vector of \link[lubridate:period]{lubridate::period} objects to add to \code{dates_of_assignment} to produce \code{success_date}.
Only used when\code{dates_of_assignment} is also supplied. Default \code{NULL}.}

\item{...}{Additional arguments forwarded to \code{time_model}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} or \code{\link[data.table:data.table]{data.table::data.table()}} with columns:
\describe{
\item{\code{id}}{Integer unit identifier.}
\item{\code{treatment}}{Treatment arm assigned to each unit.}
\item{\code{success}}{Binary outcome (0/1) drawn from a Bernoulli distribution.}
\item{\code{assignment_date}}{Date of assignment (if \code{dates_of_assignment} supplied).}
\item{\code{success_date}}{Date of outcome (if \code{dates_of_assignment} and \code{time_model} supplied).}
}
}
\description{
Simulates a randomized controlled trial with Bernoulli outcomes. Supports
complete, block, cluster, and block-and-cluster randomized assignment, optional
assignment dates, and a user-supplied time-to-event model for successful observations.
}
\examples{
# 4-Arm Design;

generate_rct.bernoulli(n = 100, p = c(0.3, 0.7, 0.5, 0.4))

# Blocked and Clustered with random probabilities

generate_rct.bernoulli(
  n = 100,
  p = list(
    Control = list(
      B1 = c(C1 = 0.6, C2 = 0.3),
      B2 = c(C3 = 0.2, C4 = 0.5)
    ),
    T1 = list(
      B1 = c(C1 = 0.8, C2 = 0.9),
      B2 = c(C3 = 0.2, C4 = 0.4)
    ),
    T2 = list(
      B1 = c(C1 = 0.3, C2 = 0.3),
      B2 = c(C3 = 0.5, C3 = 0.5)
    )
  ),
  blocks = c(B1 = 0.3, B2 = 0.7),
  clusters = list(B1 = c(C1 = 0.3, C2 = 0.7), B2 = c(C3 = 0.2, C4 = 0.8))
)

# Modelling Success Dates

time_model <- function(n, treatments, success, blocks, clusters = NULL) {
  # Specific model for each treatment and block
  time <- data.table::fcase(
    treatments == "T1" & blocks == "B1" , rexp(n, rate = 5)                 ,
    treatments == "T1" & blocks == "B2" , rexp(n, rate = 10)                ,
    treatments == "T2" & blocks == "B1" , rgamma(n, shape = 2, rate = 2)    ,
    treatments == "T2" & blocks == "B2" , rweibull(n, shape = 3, scale = 4)
  )

  time <- ifelse(success == 1, round(time) * lubridate::days(1), NA)
  return(time)
}
# Using with `single_mab_simulation()`

set.seed(100)
result <- generate_rct.bernoulli(
  n = 10000,
  p = list(
    T1 = c(B1 = 0.5, B2 = 0.3, B3 = 0.2),
    T2 = c(B1 = 0.7, B2 = 0.6, B3 = 0.9)
  ),
  blocks = c(B1 = 0.3, B2 = 0.4, B3 = 0.3),
  dates_of_assignment = lubridate::ymd("2023-04-15") +
    0:24 * months(1),
  time_model = time_model
) |>
  single_mab_simulation(
    assignment_method = "date",
    time_unit = "month",
    period_length = 1,
    algorithm = "Thompson",
    prior_periods = "All",
    perfect_assignment = FALSE,
    whole_experiment = FALSE,
    blocking = FALSE,
    data_cols = c(
      id_col = "id",
      success_col = "success",
      condition_col = "treatment",
      date_col = "assignment_date",
      assignment_date_col = "assignment_date",
      success_date_col = "success_date"
    )
  )
print(result)
result$estimates[1:3, ]
}
